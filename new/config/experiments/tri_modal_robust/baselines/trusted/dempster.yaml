defaults:
  - _evidential_base.yaml

encoder_stage:
  protocol_id: fixed_evidential_dempster_stage1_v1

method:
  name: fusion_rule_dempster
  protocol_id: fixed_evidential_dempster_v1

train:
  exp_name: fusion_rule_dempster

fusion:
  combination: dempster
  opinion_source: evidential
