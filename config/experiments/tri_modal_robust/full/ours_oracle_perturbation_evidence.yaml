defaults:
  - ./ours.yaml

train:
  exp_name: deprecated_oracle_perturbation_diagnostic_only

model:
  gate:
    # pert_* remains available in diagnostic CSVs but is forbidden as model evidence.
    use_perturbation_evidence: false
