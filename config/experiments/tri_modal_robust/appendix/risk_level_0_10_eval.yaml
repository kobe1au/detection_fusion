defaults:
  - ../seeds/seed_42.yaml

method:
  name: appendix_risk_level_0_10_eval

selective_prediction:
  mode: risk_control
  threshold_score: malware_fn_probability_anchor
  risk_target: accepted_fn_risk_among_malware
  risk_level: 0.10

eval:
  eval_only: true
  output_name: appendix_risk_level_0_10_eval
  checkpoint_path: results/tri_modal_robust/competence_anchored_seed_42/42/best_tri_modal_robust.pt
  refit_decision_calibration: true
  run_test: true
  run_robust_test: true
