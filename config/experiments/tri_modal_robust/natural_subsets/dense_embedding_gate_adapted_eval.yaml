defaults:
  - ../baselines/dense_embedding_gate_adapted.yaml
  - _subset_protocol.yaml

method:
  name: natural_subset_dense_embedding_gate_adapted_eval

train:
  eval_batch_size: 8

eval:
  eval_only: true
  output_name: natural_subset_dense_embedding_gate_adapted_eval
  checkpoint_path: results/tri_modal_robust/dense_embedding_gate_adapted/42/best_tri_modal_robust.pt
  refit_decision_calibration: false
  run_test: false
  run_robust_test: false
