defaults:
  - ../baselines/tri_modal_concat.yaml
  - _subset_protocol.yaml

method:
  name: natural_subset_tri_modal_concat_eval

train:
  eval_batch_size: 8

eval:
  eval_only: true
  output_name: natural_subset_tri_modal_concat_eval
  checkpoint_path: results/tri_modal_robust/baseline_tri_modal_concat/42/best_tri_modal_robust.pt
  refit_decision_calibration: false
  run_test: false
  run_robust_test: false
