defaults:
  - ../baselines/fixed_logit_fusion.yaml
  - _subset_protocol.yaml

method:
  name: natural_subset_fixed_logit_fusion_eval

train:
  eval_batch_size: 8

eval:
  eval_only: true
  output_name: natural_subset_fixed_logit_fusion_eval
  checkpoint_path: results/tri_modal_robust/baseline_fixed_logit_fusion/42/best_tri_modal_robust.pt
  refit_decision_calibration: false
  run_test: false
  run_robust_test: false
