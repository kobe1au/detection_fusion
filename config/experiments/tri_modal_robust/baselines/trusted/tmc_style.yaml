defaults:
  - _evidential_base.yaml

method:
  name: tmc_style_adapted

train:
  exp_name: tmc_style_adapted

fusion:
  # TMC-style adaptation: per-view Dirichlet opinions and Dempster combination,
  # using the common APK encoders and protocol rather than the original data.
  combination: dempster

