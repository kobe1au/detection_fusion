defaults:
  - _base.yaml

method:
  name: baseline_api_graph_concat

train:
  exp_name: baseline_api_graph_concat

model:
  fusion_mode: api_graph_concat

loss:
  branch_aux_weight: 0.25
  auxiliary_weight_mode: unmasked_uniform
  branch_aux_weights:
    api: 1.0
    graph: 1.0
    manifest: 0.0
