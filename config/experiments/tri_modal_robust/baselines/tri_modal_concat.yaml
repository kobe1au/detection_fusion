defaults:
  - _base.yaml

method:
  name: baseline_tri_modal_concat

train:
  exp_name: baseline_tri_modal_concat

model:
  fusion_mode: tri_modal_concat

loss:
  branch_aux_weight: 0.25
  auxiliary_weight_mode: unmasked_uniform
  branch_aux_weights:
    api: 1.0
    graph: 1.0
    manifest: 1.0
