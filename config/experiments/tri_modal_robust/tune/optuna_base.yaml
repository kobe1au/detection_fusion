defaults:
  - ../full/ours.yaml

train:
  exp_name: optuna_trial
  tuning_mode: true
  checkpoint_metric: robust_composite
  deterministic: true
  epochs: 60
  patience: 8

eval:
  run_test: false
  run_robust_test: false
  robust_val:
    enabled: true
