defaults:
  - ../observable_reliability_discount_fusion.yaml

train:
  exp_name: no_discount_fusion

model:
  fusion_mode: tri_modal_ours

fusion:
  mode: legacy_learned_gate

calibration:
  enabled: false
  # Keep checkpoint selection on the same validation subset as the final method.
  holdout_enabled: true

selective_prediction:
  enabled: false
