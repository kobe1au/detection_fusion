defaults:
  - ../observable_reliability_discount_fusion.yaml

train:
  exp_name: high_masked_semantic_reconstruction

semantic_reconstruction:
  enabled: true
  weight: 0.05
  mask_prob_api: 0.3
  mask_prob_graph: 0.3
  mask_prob_manifest: 0.3

