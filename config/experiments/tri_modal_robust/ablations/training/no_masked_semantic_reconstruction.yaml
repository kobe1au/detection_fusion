defaults:
  - ../../observable_reliability_discount_fusion.yaml

method:
  name: training_no_masked_semantic_reconstruction

train:
  exp_name: training_no_masked_semantic_reconstruction

semantic_reconstruction:
  enabled: false
  weight: 0.0

semantic_cross_attention:
  attach_to_reconstruction: false
