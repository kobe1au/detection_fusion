defaults:
  - ../../evidential_trusted_fusion.yaml

# Training ablation: EDL without class weighting (tests the imbalance guard).
method:
  name: training_no_edl_class_weight

train:
  exp_name: training_no_edl_class_weight

loss:
  evidential:
    class_weight: null
