defaults:
  - ../../observable_reliability_discount_fusion.yaml

method:
  name: mechanism_no_probability_calibration

train:
  exp_name: mechanism_no_probability_calibration

fusion:
  probability_calibration:
    enabled: false
