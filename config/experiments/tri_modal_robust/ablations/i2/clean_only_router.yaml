defaults:
  - ../../seeds/seed_42.yaml

method:
  name: ablation_i2_clean_only_router

train:
  exp_name: ablation_i2_clean_only_router

encoder_stage:
  mode: reuse
  checkpoint_path: results/tri_modal_robust/competence_anchored_seed_42/42/best_encoder_selected.pt
  strict_identity: true

stage_b:
  router:
    degradation_loss_weights: [0.0]
