defaults:
  - ../../evidential_trusted_fusion.yaml

# I2 ablation: Jøsang cumulative belief fusion (associative, accumulates
# evidence). An alternative conflict-tolerant rule for comparison with Yager.
method:
  name: ablation_combination_cumulative

train:
  exp_name: ablation_combination_cumulative

fusion:
  combination: cumulative
