defaults:
  - ../../evidential_trusted_fusion.yaml

method:
  name: i2_scenario_weight_clean_0_70

train:
  exp_name: i2_scenario_weight_clean_0_70

fusion:
  routing:
    # Sensitivity cell: deployment objective puts more mass on clean inputs.
    scenario_objective_weights:
      clean: 0.7
      perturb: 0.3
