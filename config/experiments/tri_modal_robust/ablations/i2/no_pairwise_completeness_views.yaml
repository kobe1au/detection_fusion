defaults:
  - ../../evidential_trusted_fusion.yaml

method:
  name: i2_no_pairwise_completeness_views

train:
  exp_name: i2_no_pairwise_completeness_views

calibration:
  # Keep the subset oracle and soft-worst objective unchanged; remove only the
  # three API+Graph/API+Manifest/Graph+Manifest route-training views.
  include_pairwise_completeness_views: false

