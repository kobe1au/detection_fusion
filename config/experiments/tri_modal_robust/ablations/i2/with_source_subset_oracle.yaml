defaults:
  - ../../evidential_trusted_fusion.yaml

method:
  name: i2_with_source_subset_oracle

train:
  exp_name: i2_with_source_subset_oracle

fusion:
  routing:
    # Optional add-on only. It tests whether source-level subset distillation
    # adds value beyond the deployable sample-conditional mixture objective.
    subset_oracle_loss_weight: 0.25
    subset_oracle_temperature: 0.25
