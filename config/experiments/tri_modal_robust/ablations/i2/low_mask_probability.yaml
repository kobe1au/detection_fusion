defaults:
  - ../../observable_reliability_discount_fusion.yaml

train:
  exp_name: i2_low_mask_probability

semantic_reconstruction:
  mask_prob_api: 0.05
  mask_prob_graph: 0.05
  mask_prob_manifest: 0.05
