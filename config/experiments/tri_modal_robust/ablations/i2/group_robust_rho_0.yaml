defaults:
  - ../../evidential_trusted_fusion.yaml

method:
  name: i2_group_robust_rho_0

train:
  exp_name: i2_group_robust_rho_0

fusion:
  routing:
    group_robust_objective:
      # Same perturb-type hierarchy, without the soft worst-group component.
      soft_worst_weight: 0.0

