defaults:
  - ../../evidential_trusted_fusion.yaml

# I2 mechanism: Product-of-experts / log opinion pool. No subjective-logic conflict handling; a simpler probabilistic-pooling baseline for I2.
method:
  name: ablation_combination_log_pool

train:
  exp_name: ablation_combination_log_pool

fusion:
  combination: log_pool
