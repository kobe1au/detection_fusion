defaults:
  - ../../evidential_trusted_fusion.yaml

method:
  name: i2_group_robust_family_taxonomy

train:
  exp_name: i2_group_robust_family_taxonomy

fusion:
  routing:
    group_robust_objective:
      # Sensitivity cell: balance five paper-level robustness families before
      # applying the same stateless soft worst-family reduction.
      taxonomy: robustness_family_v1

