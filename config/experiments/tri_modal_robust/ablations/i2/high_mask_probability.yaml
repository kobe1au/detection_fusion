defaults:
  - ../../observable_reliability_discount_fusion.yaml

train:
  exp_name: i2_high_mask_probability

semantic_reconstruction:
  mask_prob_api: 0.30
  mask_prob_graph: 0.30
  mask_prob_manifest: 0.30
