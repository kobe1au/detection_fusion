defaults:
  - ../../evidential_trusted_fusion.yaml

method:
  name: i2_combination_dempster

train:
  exp_name: i2_combination_dempster

fusion:
  combination: dempster
  routing:
    enabled: false
    acceptance_score_mode: product
    # Fit the same single clean-validation scalar used by the routed method so
    # NLL/ECE differences reflect the combination rule, not unequal calibration.
    final_temperature_scaling: true
