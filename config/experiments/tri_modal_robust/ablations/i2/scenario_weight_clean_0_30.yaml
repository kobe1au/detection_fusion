defaults:
  - ../../evidential_trusted_fusion.yaml

method:
  name: i2_scenario_weight_clean_0_30

train:
  exp_name: i2_scenario_weight_clean_0_30

fusion:
  routing:
    # Sensitivity cell: deployment objective puts more mass on perturbations.
    scenario_objective_weights:
      clean: 0.3
      perturb: 0.7
