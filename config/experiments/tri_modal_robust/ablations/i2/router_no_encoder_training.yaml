defaults:
  - ../../evidential_trusted_fusion.yaml

method:
  name: i2_router_no_encoder_training

train:
  exp_name: i2_router_no_encoder_training

fusion:
  routing:
    # The router stays at its smooth prior initialization during encoder
    # training and is learned only on the independent post-hoc subset.
    train_end_to_end: false

