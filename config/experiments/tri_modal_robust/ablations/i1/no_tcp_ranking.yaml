defaults:
  - ../../seeds/seed_42.yaml

method:
  name: ablation_i1_no_tcp_ranking

train:
  exp_name: ablation_i1_no_tcp_ranking

encoder_stage:
  mode: reuse
  checkpoint_path: results/tri_modal_robust/competence_anchored_seed_42/42/best_encoder_selected.pt
  strict_identity: true

stage_b:
  competence:
    ranking_weight: 0.0
