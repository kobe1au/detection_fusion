defaults:
  - ../../observable_reliability_discount_fusion.yaml

train:
  exp_name: i1_no_support_evidence

model:
  gate:
    use_consistency_evidence: false

semantic_cross_attention:
  use_support_bias: false

fusion:
  use_support_discount: false
