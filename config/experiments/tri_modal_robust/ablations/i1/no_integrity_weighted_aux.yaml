defaults:
  - ../../evidential_trusted_fusion.yaml

method:
  name: i1_no_integrity_weighted_aux

train:
  exp_name: i1_no_integrity_weighted_aux

loss:
  # Atomic training-objective check. I1 calibration, reliability use, the
  # visibility modifier, I2 routing, and I3 remain identical to the full method.
  integrity_weighted_aux: false

