defaults:
  - ../../evidential_trusted_fusion.yaml

method:
  name: i1_no_evidential_certainty

train:
  exp_name: i1_no_evidential_certainty

fusion:
  reliability_calibration:
    # Margin-only continuous signal.
    use_evidential_certainty: false
