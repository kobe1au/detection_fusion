defaults:
  - ../../evidential_trusted_fusion.yaml

method:
  name: i1_no_prediction_margin

train:
  exp_name: i1_no_prediction_margin

fusion:
  reliability_calibration:
    # Vacuity-only continuous signal.
    use_prediction_margin: false
