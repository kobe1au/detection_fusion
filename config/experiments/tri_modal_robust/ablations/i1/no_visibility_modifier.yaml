defaults:
  - ../../evidential_trusted_fusion.yaml

method:
  name: i1_no_visibility_modifier

train:
  exp_name: i1_no_visibility_modifier

fusion:
  # Keep the clean correctness calibrator unchanged and remove only the
  # parameter-free deployment-time relative-effective-integrity modifier.
  visible_integrity_modifier:
    enabled: false

