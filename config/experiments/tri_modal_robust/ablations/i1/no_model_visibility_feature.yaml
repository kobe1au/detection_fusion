defaults:
  - ../../evidential_trusted_fusion.yaml

method:
  name: i1_no_model_visibility_feature

train:
  exp_name: i1_no_model_visibility_feature

fusion:
  reliability_calibration:
    # The fixed superset layout masks this slot to zero, so the calibrator and
    # router parameter topology and RNG consumption remain unchanged.
    use_model_visibility: false

