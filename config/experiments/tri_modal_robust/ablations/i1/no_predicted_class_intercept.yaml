defaults:
  - ../../evidential_trusted_fusion.yaml

method:
  name: i1_no_predicted_class_intercept

train:
  exp_name: i1_no_predicted_class_intercept

fusion:
  reliability_calibration:
    # Remove only the optional benign/malware conditional intercept.
    use_predicted_class_intercept: false
