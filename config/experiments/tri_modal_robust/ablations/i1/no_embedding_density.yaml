defaults:
  - ../../evidential_trusted_fusion.yaml

method:
  name: i1_no_embedding_density

train:
  exp_name: i1_no_embedding_density

fusion:
  reliability_calibration:
    # Fixed topology and identical calibration rows: zero-mask only the
    # branch-local density slot so this remains a true one-axis I1 ablation.
    use_embedding_density: false
