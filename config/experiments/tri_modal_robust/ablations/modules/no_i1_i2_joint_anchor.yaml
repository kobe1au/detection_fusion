defaults:
  - ../../seeds/seed_42.yaml

method:
  name: module_no_i1_i2_joint_anchor

train:
  exp_name: module_no_i1_i2_joint_anchor

# Reuse the exact proposed-method Stage-A artifact and disable the complete
# competence/router stage. This is the causal I1+I2 module ablation; a
# separately trained concat baseline is not an equivalent substitute.
encoder_stage:
  mode: reuse
  checkpoint_path: results/tri_modal_robust/competence_anchored_seed_42/42/best_encoder_selected.pt
  strict_identity: true

stage_b:
  enabled: false
