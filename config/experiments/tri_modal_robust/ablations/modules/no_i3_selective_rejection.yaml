defaults:
  - ../../evidential_trusted_fusion.yaml

# I3 OFF: full-coverage prediction, no conformal abstention.
method:
  name: module_no_i3_selective_rejection

train:
  exp_name: module_no_i3_selective_rejection

selective_prediction:
  enabled: false
