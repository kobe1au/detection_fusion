defaults:
  - ../../evidential_trusted_fusion.yaml

method:
  name: sensitivity_visible_modifier_beta_0_5

train:
  exp_name: sensitivity_visible_modifier_beta_0_5

fusion:
  visible_integrity_modifier:
    enabled: true
    beta: 0.5
    min_value: 0.5
    min_reference: 1.0e-6