defaults:
  - ../../evidential_trusted_fusion.yaml

method:
  name: sensitivity_reliability_hidden_dim_32

train:
  exp_name: sensitivity_reliability_hidden_dim_32

fusion:
  reliability_calibration:
    hidden_dim: 32
