defaults:
  - ../../observable_reliability_discount_fusion.yaml

method:
  name: sensitivity_reliability_acceptance_only

train:
  exp_name: sensitivity_reliability_acceptance_only

fusion:
  # Keep observable reliability for selective rejection, but do not multiply it
  # into branch fusion weights.
  use_reliability_discount: false
  use_reliability_acceptance: true
