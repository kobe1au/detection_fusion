defaults:
  - ../_tuning_train_base.yaml

method:
  name: tuning_objective_semantic_reconstruction_weight_0_04

train:
  exp_name: tuning_objective_semantic_reconstruction_weight_0_04

semantic_reconstruction:
  weight: 0.04
