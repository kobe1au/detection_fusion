defaults:
  - _tuning_train_base.yaml

method:
  name: tuning_final_candidate

train:
  exp_name: tuning_final_candidate

fusion:

  acceptance_aggregation: product
  
  conflict_factor:
    min_value: 0.2

semantic_cross_attention:
  dropout: 0.0