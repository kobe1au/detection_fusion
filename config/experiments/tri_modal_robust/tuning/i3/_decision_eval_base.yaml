defaults:
  - ../../observable_reliability_discount_fusion.yaml

train:
  exp_name: tuning_i3_decision_eval

eval:
  eval_only: true
  checkpoint_path: results/tri_modal_robust/tuning_full_candidate/42/best_tri_modal_robust.pt
  refit_rejection_threshold: true
  run_test: false
  run_robust_test: false
  robust_val:
    enabled: false
